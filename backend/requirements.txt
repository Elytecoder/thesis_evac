Django>=4.2,<5
Pillow>=10.0
djangorestframework>=3.14
django-cors-headers>=4.0
whitenoise>=6.5
gunicorn>=21.0
# Token auth is built into DRF (rest_framework.authtoken)
# For Random Forest (optional; falls back to constant risk if not installed)
scikit-learn>=1.3
# numpy is auto-installed by scikit-learn
# PostgreSQL adapter — used when DATABASE_URL env var is set (Render production)
psycopg2-binary>=2.9
# Parses DATABASE_URL into Django DATABASES dict
dj-database-url>=2.1
# Testing
pytest>=7.0
pytest-django>=4.5
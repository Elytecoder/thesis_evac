// Smoke test — verifies the app can build without throwing.
// The default counter test was removed because this project does not use
// the Flutter counter template.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — app has no unit-testable logic at the widget level yet', () {
    expect(1 + 1, 2);
  });
}
